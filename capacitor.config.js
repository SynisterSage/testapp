// capacitor.config.js
/** @type {import('@capacitor/cli').CapacitorConfig} */
const config = {
  appId: "com.synistersag.overtone",
  appName: "Overtone",
  webDir: "dist" // 👈 must match your build output folder
};
export default config;
