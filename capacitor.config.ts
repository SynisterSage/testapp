import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.synistersage.overtone',
  appName: 'Overtone',
  webDir: 'dist',
  // (optional) For live-reload via LAN dev server, uncomment and set your LAN IP:
  // server: { url: 'http://192.168.1.123:5173', cleartext: true }
};

export default config;
